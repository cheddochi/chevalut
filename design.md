# chevault 웹앱 디자인 가이드

> obsidian.md 공식 사이트의 시각적 분위기(다크 테마, 퍼플 그라디언트, 그래프 뷰 모티프)를
> chevault 웹 대시보드에 적용하기 위한 디자인 방향 문서.
> planning.md 6번 섹션("그래프+카드 하이브리드형" 웹앱 프론트엔드)의 비주얼 스펙 역할.

---

## 1. 톤 앤 매너 (Tone & Manner)

오브시디언의 사이트는 "화려한 마케팅 사이트"가 아니라 **조용하고 차분한 도구의 인상**을 준다.
- 미니멀, 여백 많음, 텍스트 위주 — 불필요한 장식 없음
- 다크 배경 기반의 차분한 톤, 포인트 컬러(퍼플)는 아껴서 사용
- "로컬 우선(local-first)", "당신의 노트는 당신 것" 같은 개발자/파워유저 친화적 신뢰감
- 그래프 뷰(지식 네트워크 시각화)가 브랜드의 핵심 아이덴티티 — 화려하지 않지만 인상적인 "은은하게 빛나는 점과 선"

chevault 웹앱에도 동일한 원칙을 적용: **정보량이 많아도 산만해 보이지 않게**, 카드/그래프 모두
어두운 배경 위에 "떠 있는" 느낌으로 구성한다.

---

## 2. 컬러 팔레트

### 2-1. 베이스 (다크 모드 기본)

| 용도 | 색상 | 설명 |
|---|---|---|
| 배경 (기본) | `#1e1e1e` ~ `#202020` | 완전한 검정이 아닌 짙은 차콜 — 오브시디언 특유의 "따뜻한 다크" |
| 배경 (서브/카드) | `#262626` ~ `#2a2a2a` | 카드/패널은 배경보다 한 톤 밝게 — 미세한 레이어감 |
| 배경 (오버레이/모달) | `#161616` | 배경보다 한 톤 어둡게, 깊이감 |
| 텍스트 (기본) | `#dcddde` | 순백이 아닌 약간 톤 낮춘 회백색 (눈 피로 감소) |
| 텍스트 (보조/muted) | `#9a9a9a` ~ `#808080` | 메타정보, 타임스탬프, placeholder |
| 테두리/구분선 | `#3a3a3a` (subtle) | 거의 안 보이지만 카드 경계를 은은하게 구분 |

### 2-2. 포인트 컬러 (오브시디언 시그니처 퍼플)

| 용도 | 색상 | 설명 |
|---|---|---|
| 브랜드 퍼플 | `#7c3aed` (base) | 오브시디언 아이콘/CTA의 핵심 보라색 |
| 그라디언트 A | `#8a4fff → #b76bff` | 버튼/헤드라인 강조용 라이트 퍼플 그라디언트 |
| 그라디언트 B (그래프용) | `#7c3aed → #d946ef` | 퍼플→마젠타/핑크, 그래프 노드 하이라이트용 |
| 링크/인터랙션 | `#a48fff` | 다크 배경 위에서 대비가 좋은 라이트 퍼플 |
| 성공/카테고리 강조 (보조) | `#4ade80`(연두), `#38bdf8`(하늘) 등 | 카테고리별 구분색은 채도 낮은 톤으로 최대 5~6개 정도만 |

**원칙: 퍼플은 CTA·활성 상태·그래프 강조 노드에만 쓰고, 나머지 UI는 그레이스케일로 절제한다.**
카테고리별 색상은 채도를 낮춰서 퍼플이 항상 시각적으로 "가장 튀는 색"이 되도록 유지.

### 2-3. 라이트 모드 (보조)

다크 모드가 기본이지만 라이트 모드도 톤을 맞춘다.
- 배경 `#faf8f5`(약간 웜톤 오프화이트) / 카드 `#ffffff` / 텍스트 `#2e2e2e` / 포인트 컬러는 다크모드와 동일한 퍼플 계열 유지(채도만 살짝 낮춤, `#8b5cf6`).

---

## 3. 타이포그래피

- **본문/UI**: 시스템 산세리프 기반 (`Inter`, `-apple-system`, `Segoe UI` 등) — 오브시디언 사이트도 특수 커스텀 폰트보다 깔끔한 산세리프를 씀.
- **헤딩**: 굵고 큼직하게, 자간은 살짝 좁게(-0.02em). 화려한 장식체 대신 **웨이트 대비**로 임팩트를 줌 (예: 700~800 굵기의 헤드라인 + 400 굵기의 서브텍스트).
- **모노스페이스 포인트**: 노트 경로, 태그, 코드/frontmatter 표시 등 "기술적 요소"에는 `JetBrains Mono`/`Fira Code` 같은 모노스페이스를 부분적으로 사용해 개발자 도구 느낌을 살림.
- **행간(line-height)**: 본문 1.6~1.7 — 여백을 넉넉히 둬서 텍스트가 답답해 보이지 않게.
- **크기 스케일**: 12 / 14 / 16(base) / 20 / 28 / 40 / 56px 정도의 절제된 스케일. 히어로 타이틀만 예외적으로 크게(48~64px).

---

## 4. 레이아웃 & 컴포넌트

### 4-1. 홈 화면 (카드 클러스터)

- 상단: 크지 않은 헤더 + 검색창 (검색창은 중앙이 아니라 상단 고정, 눈에 띄지 않게 절제된 스타일 — cmd+k 팔레트 느낌)
- 카테고리(최상위 폴더)별로 **카드 클러스터** 배치:
  - 카드 배경 `#262626`, radius `12px`, 얇은 테두리(`1px solid #3a3a3a`)
  - 호버 시 아주 미묘한 상승 효과 + 테두리가 퍼플 계열로 살짝 밝아짐 (`box-shadow`에 퍼플 글로우 소량 추가)
  - 카드 안에는: 카테고리명(굵게), 노트 개수, 최근 수정 노트 3~4개 미리보기(제목만, muted 텍스트)
- 카드 그리드는 여백을 넉넉히 (gap 24px 이상) — 오브시디언 사이트 특유의 "숨 쉬는 여백"

### 4-2. 그래프 뷰 (탐색)

이게 오브시디언 브랜드의 핵심 시각 요소이므로 가장 공들여야 함.
- 배경은 순수 다크(`#161616`), 그 위에 **노드 = 은은하게 빛나는 점**, **엣지 = 얇고 반투명한 선**
- 노드 색상: 기본은 muted gray(`#666`), 현재 보고 있는/선택된 노트는 퍼플 글로우(`#a48fff`, box-shadow blur)로 강조
- 링크(엣지)는 `stroke-opacity: 0.15~0.3` 정도로 흐릿하게 — 너무 많은 선이 겹쳐도 시각적으로 무겁지 않게
- 카테고리별로 노드 클러스터가 자연스럽게 뭉치도록 force-directed 레이아웃(d3-force 등) 사용
- 줌/팬 인터랙션, 노드 클릭 시 사이드 패널에 해당 노트 미리보기 슬라이드 인
- **note_links의 broken link(target_note_id가 NULL)** 는 점선 + 낮은 opacity로 구분 표시

### 4-3. 검색/탐색 UI

- "검색보다 탐색"이라는 컨셉에 맞게, 검색창은 눈에 띄지만 위압적이지 않게 — 단축키(`⌘K`) 힌트를 옅게 표시
- 태그는 알약(pill) 형태, 배경 `#333`에 텍스트 `#a48fff` — 클릭 시 필터링
- 전문 검색 결과는 카드가 아니라 리스트형(제목 + 카테고리 배지 + 스니펫), 매칭 키워드만 퍼플로 하이라이트

### 4-4. 버튼 / CTA

- Primary: 퍼플 그라디언트 배경(`#8a4fff → #b76bff`), radius는 큰 편(pill에 가까운 8~10px), 흰 텍스트, 은은한 그림자
- Secondary/Ghost: 배경 없음, `1px solid #3a3a3a` 테두리, 텍스트만 밝은 회색 — 호버 시 테두리만 퍼플로 전환
- 버튼 텍스트는 담백하게 ("노트 열기", "그래프에서 보기") — 과장된 마케팅 카피 지양

---

## 5. 아이콘 / 그래픽 스타일

- 라인 아이콘(획 두께 1.5~2px, 채우기 없음) 위주 — Lucide/Feather 계열과 톤이 잘 맞음
- 카테고리 아이콘은 폴더 이모지 대신 심플한 라인 아이콘 + muted 컬러
- 일러스트 대신 **그래프 시각화 자체가 메인 그래픽 요소** — 별도 장식 일러스트는 최소화

---

## 6. 다크 모드 우선(Dark-first) 원칙

- 기본 진입은 다크 모드, 라이트 모드는 토글로 제공
- 모든 컴포넌트를 다크 배경 기준으로 먼저 디자인하고, 라이트 모드는 반전이 아니라 **톤을 맞춘 별도 팔레트**로 대응 (2-3 참고)

---

## 7. 요약 체크리스트 (구현 시 참고)

- [ ] 배경: 차콜(`#1e1e1e`) 기반, 카드/패널은 한 톤 밝게
- [ ] 포인트 컬러는 퍼플 하나로 통일, 아껴서 사용
- [ ] 그래프 뷰: 은은한 글로우 노드 + 반투명 엣지 + force-directed 레이아웃
- [ ] 타이포: 산세리프 본문 + 모노스페이스 포인트(경로/태그) + 절제된 크기 스케일
- [ ] 여백을 넉넉히, 카드 radius 12px, 테두리는 거의 안 보일 정도로 subtle
- [ ] 버튼은 퍼플 그라디언트(primary) / 아웃라인(secondary) 두 종류로 단순화
- [ ] 마케팅 톤 배제, 담백하고 기술적인 카피

---

*참고: 이 문서는 obsidian.md에 대한 일반적으로 알려진 브랜드 아이덴티티(다크 테마, 퍼플 그라디언트,
그래프 뷰 시각화)를 기반으로 작성되었으며, 이 세션에서는 네트워크 egress 제한으로 사이트를
직접 스크래핑하지 못했습니다. 실제 구현 전에 obsidian.md를 직접 열어 최신 컬러 hex 값/폰트를
브라우저 개발자 도구로 한 번 더 확인해 미세 조정하는 것을 권장합니다.*
