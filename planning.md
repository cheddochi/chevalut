# Obsidian Summary Agent planning.md

## 1. 프로젝트 목적

옵시디언에 쌓아온 개인 문서(md 파일)를 자동으로 읽어, 그 안의 내용을 "단일 사건" 단위 문장으로
정리하고 태그와 카테고리를 붙여 저장한다. 목적은 나중에 필요한 정보를 원문 md를 다시 뒤지지 않고
정리된 형태로 빠르게 찾아보는 것이다.

옵시디언 볼트는 Obsidian Sync + S3 Sync 플러그인을 통해 **Cloudflare R2 오브젝트 스토리지**
(`chevault` 버킷의 `vault/` 접두사)에 실시간에 가깝게 동기화되고 있다. 이 프로젝트는 R2의 md
원본을 기본 데이터 소스로 삼아 분석을 자동 수행하는 것을 우선으로 하고, md 파일 업로드나 클립보드
붙여넣기는 아직 동기화 전이거나 즉석에서 분석하고 싶은 내용을 넣는 보조 입력 경로로 둔다.

> 이전 버전에서는 "Postgres `notes` 테이블에 원본이 미러링되어 있다"고 가정했으나, 실제로는
> 원본이 R2에만 있고 그런 미러링 테이블은 없는 것으로 확인되어 R2를 직접 읽는 구조로 정정했다.

## 2. 주요 사용자

- 나 혼자 사용하는 개인용 도구. 다중 사용자 인증/권한 구분은 필요 없다.

## 3. 해결하려는 문제

- 옵시디언 md 노트는 원문 그대로라 나중에 다시 읽으며 필요한 부분을 찾아야 해서 시간이 든다.
- 하나의 md 파일 안에 여러 사건/내용이 섞여 있어, 검색해도 원하는 부분만 골라보기 어렵다.
- 태그·카테고리를 매번 손으로 붙이기 번거롭다.
- 볼트는 이미 R2로 자동 동기화되고 있지만, 그 원문 자체는 여전히 정리되지 않은 상태로 쌓이기만
  할 뿐 분석·요약되지 않는다.
- 문장이 몇 백 개 단위로 쌓이면 문장을 하나하나 리스트로 읽는 것보다, 태그 빈도와 태그 간 연관
  관계로 먼저 훑어보고 필요한 것만 찾아 들어가는 탐색 방식이 더 유용하다.

## 4. 꼭 필요한 기능

1. **(기본)** Cloudflare R2(`chevault/vault/...`)에서 새로 추가되거나 변경된 md 노트를 찾아
   분석 대상으로 삼는다. R2 오브젝트의 마지막 수정 시각을 기준으로 중복 분석을 방지한다.
2. **(보조)** md 파일 업로드 또는 클립보드 붙여넣기로 개별 노트 내용을 직접 분석 대상으로 넣는다.
3. md 내용을 "단일 사건" 단위 문장으로 파싱해서 저장
4. 파싱된 문장마다 태그 자동 생성
5. 파싱된 문장마다 카테고리 자동 분류
5-1. **개인정보 보호**: 문장·태그 생성 시 특정 개인의 실명, 차량 번호판, 회사/기관 실명이
     그대로 남지 않도록 자동으로 가려서(일반적인 역할·표현으로 대체) 저장한다.
6. **태그 목록 보기**: 문장을 그대로 나열하지 않고, 태그별 사용 빈도와 그 태그와 자주 함께
   등장하는 1차 연관 태그 정보를 먼저 보여준다.
7. **태그 검색(자동완성)**: 목록 화면 검색창에 타이핑할 때마다 포털 검색엔진처럼 일치하는 태그를
   실시간으로 추천 목록에 띄우고, 하나를 선택하면 그 태그를 중심으로 한 결과(관련 문장 목록)를
   보여준다. 이 선택은 토폴로지 보기에도 그대로 반영(동기화)된다.
8. 분석된 문장·태그·카테고리 관계를 토폴로지(네트워크) 형태로 시각화해 한눈에 내용을 식별
9. **토폴로지 인터랙션**: 화면 진입 시 모든 노드가 잘리지 않고 한 화면에 들어오도록 자동으로
   맞춰서 보여주고, 라벨 글자 크기는 겹치지 않고 읽기 좋은 수준으로 유지한다. 노드 간 퍼지는
   정도(중력/척력)를 슬라이더로 직접 조정할 수 있다. 확대·축소는 버튼으로만 하고, **마우스
   휠 스크롤로는 확대·축소되지 않게** 막아 스크롤 중 실수로 확대되는 걸 방지한다. 드래그로는
   화면 이동이 가능하다. 노드를 클릭하면 관련된 md 노트 목록이 뜨고, 그중 하나를 선택하면
   해당 노트의 전체 내용을 화면에서 볼 수 있다.

## 5. 필요한 화면

- **입력 화면**: (보조 경로) md 파일 업로드 또는 내용 붙여넣기로 개별 노트 분석 실행.
- **결과 확인 화면 — 목록 보기**:
  - 기본 상태: 태그 목록(빈도순 정렬) + 각 태그의 1차 연관 태그를 함께 표시.
  - 검색창에 입력 시 일치하는 태그를 자동완성 형태로 추천, 선택하면 해당 태그로 필터링된
    문장 목록(태그/카테고리/원본 노트 경로 포함)을 보여준다.
- **결과 확인 화면 — 토폴로지 보기**: 문장·태그·카테고리를 노드로, 문장-태그·문장-카테고리
  관계를 연결선으로 표시. 목록 보기에서 태그를 선택한 상태라면 토폴로지도 그 태그 중심의
  부분 그래프만 보여준다(동기화). 확대/축소/드래그 이동 가능, 노드 클릭 시 관련 노트 목록 →
  노트 선택 시 전체 내용 표시.
- 목록 보기 / 토폴로지 보기는 한 화면에서 탭으로 전환 가능하게 구성.

### 원하는 화면 분위기

첨부된 `design.md`(오브시디언 공식 사이트 톤 기반 디자인 가이드)의 방향을 반영한다.
목록 보기는 색/타이포/톤 위주로, 토폴로지 보기는 design.md의 그래프 뷰(4-2) 스타일을 그대로 적용한다.

- **톤 앤 매너**: 화려한 마케팅 톤이 아니라 조용하고 차분한 도구 느낌. 미니멀, 여백 많음, 불필요한 장식 없음.
- **베이스 컬러(다크 모드 기본)**: 배경은 순검정이 아닌 짙은 차콜(`#1e1e1e`~`#202020`), 카드/패널은
  한 톤 밝은 `#262626`~`#2a2a2a`, 기본 텍스트는 순백 대신 톤 낮춘 회백색(`#dcddde`), 보조 텍스트는
  muted gray(`#9a9a9a`), 테두리는 거의 안 보이는 `#3a3a3a`.
- **포인트 컬러**: 퍼플(`#7c3aed` 베이스, 버튼/헤드라인용 그라디언트 `#8a4fff → #b76bff`)을
  강조·CTA·태그 하이라이트에만 아껴서 사용하고, 나머지 UI는 그레이스케일로 절제.
- **타이포그래피**: 본문/UI는 시스템 산세리프(`Inter` 등), 헤딩은 굵게 웨이트 대비로 임팩트.
  태그·원문 경로 같은 기술적 요소는 모노스페이스(`JetBrains Mono` 등)로 포인트.
- **버튼**: Primary(파싱 실행 등)는 퍼플 그라디언트 배경, Secondary는 배경 없이 테두리만.
  담백한 카피 사용, 과장된 마케팅 문구 지양.
- **토폴로지 보기 스타일**: 배경은 순수 다크(`#161616`), 노드(문장/태그/카테고리)는 은은하게
  빛나는 점으로, 연결선은 얇고 반투명하게 표시. 카테고리별로 노드가 자연스럽게 뭉치도록
  force-directed 레이아웃을 사용하되, 항상 전체가 한 화면에 들어오도록 자동으로 맞춘다.
- **모드**: 다크 모드를 기본값으로 하고 시작한다(라이트 모드 토글 등 테마 커스터마이징은 8번에
  따라 만들지 않음 — 다크 테마 하나만 고정 적용).

## 6. 저장할 정보

- 단일 사건 문장 (원문에서 파싱된 문장 텍스트) + 카테고리
- 태그 (문장별 자동 생성된 태그, 다대다 관계)
- 원본 노트 참조 정보 — vault 소스는 R2 오브젝트 키(예: `vault/01.../회의록.md`, 중복 분석
  방지용 유니크 키), manual(업로드·붙여넣기) 소스는 제목 + 원문 자체를 보관 (R2에 없으므로)
- 마지막으로 분석을 완료한 노트의 R2 오브젝트 수정 시각 — 다음 자동 실행에서 중복 분석 방지 기준

원본 md 내용 자체는 R2에만 있으므로 vault 소스는 원문을 다시 저장하지 않는다. 분석 결과
(문장/태그/카테고리와 원본 참조)는 Hyperdrive를 통해 PlanetScale Postgres(`chevalut2`)의
`summary_*` 테이블에 저장한다 (별도 D1 등 다른 저장소 없음).

## 7. 사용자가 서비스를 이용하는 순서

1. **(기본)** 별도 조작 없이도, "동기화된 노트 분석" 실행 시 R2 `vault/`에 새로 들어오거나
   변경된 노트를 찾아 자동으로 분석한다. 같은 배치 로직이 10분마다 cron으로도 자동 실행된다.
2. **(보조)** 특정 노트를 바로 분석하고 싶으면 md 파일을 업로드하거나 내용을 붙여넣어
   수동으로 분석을 실행할 수 있다 (팝업으로 제공).
2-1. **(관리용)** "전체 강제 재분석" 버튼으로 기존 분석 결과를 전부 지우고 볼트 전체를
     처음부터 다시 분석할 수 있다 (프롬프트 개선 후 전체 데이터를 새로 만들 때 사용,
     확인 대화상자를 거치며 Workers AI 할당량을 크게 소모할 수 있음을 안내한다).
3. 분석이 실행되면 서비스가 내용을 단일 사건 단위 문장으로 분리하고, 태그·카테고리를 붙여 저장한다.
4. 목록 보기에서 태그 빈도 목록을 훑어보거나, 검색창에 타이핑해 원하는 태그를 자동완성으로 찾는다.
5. 태그를 선택하면 그 태그를 중심으로 한 문장 목록을 확인한다.
6. 토폴로지 보기로 전환하면 같은 선택이 반영된 관계도를 확인할 수 있고, 노드를 클릭해 관련
   노트를 찾고 전체 내용을 열어볼 수 있다.

## 8. 이번에 만들지 않을 기능

- 여러 사용자를 위한 로그인/권한 관리
- R2 변경 즉시 반응하는 실시간 스트리밍 분석 (수동 실행 버튼으로 여러 번 나눠 호출하는 방식으로 충분)
- 옵시디언 위키링크 기반 노트 간 관계 그래프 (이 프로젝트는 위키링크를 다루지 않음 —
  문장·태그·카테고리 관계를 보여주는 토폴로지 시각화는 4번 기능으로 포함됨)
- 태그·카테고리를 손으로 고치는 편집 UI (자동 생성 결과만 저장)
- 전문 검색(본문 전체 텍스트 검색) 등 태그 검색 이외의 고급 검색 기능
- 라이트 모드 토글 등 테마 커스터마이징 기능 (다크 테마 하나만 고정 적용)

## 9. 기술 및 배포 방향

- Cloudflare Worker 하나로 구성. **R2 바인딩**(`chevault` 버킷, `vault/` 접두사)으로 원본 md를
  읽고, **Hyperdrive 바인딩**으로 분석 결과를 PlanetScale Postgres(`chevalut2`)의 `summary_*`
  테이블에 저장한다. 별도 D1/KV 등 다른 저장소는 두지 않는다.
- 문장 분리·태그·카테고리 생성은 **Cloudflare Workers AI**(`@cf/meta/llama-3.1-8b-instruct-fp8`)
  바인딩으로 처리한다. 처음엔 `llama-3.3-70b-instruct-fp8-fast`를 썼으나, 일일 무료 뉴런
  할당량(10,000개)으로 볼트 159개를 이틀 연속 다 못 돌릴 만큼 단가가 비싸(출력 기준 8b-fp8의
  약 7.9배) 8b 모델로 낮췄다 — 한국어 태그/카테고리 추출 수준에는 충분하다고 판단.
  Workers AI는 계정당 일일 무료 뉴런 할당량이 있고, 다 쓰면 그날은 추가 분석이 막힌다(모델을
  낮춰도 그날의 소진된 할당량 자체는 되돌아오지 않음 — Paid 플랜 업그레이드나 다음 날 리셋 필요).
- 시스템 프롬프트에 개인정보(실명/차량번호/회사명) 가리기 지시를 포함해, 분석 시점에 바로
  가려진 상태로 문장·태그가 생성되게 한다. `POST /api/admin/wipe`로 기존 분석 결과를 전부
  지우고 재분석을 트리거할 수 있다(예: 프롬프트를 바꿔 예전 데이터를 새로 걸러 다시 만들 때).
- 프로젝트 파일과 변경 내용은 GitHub 저장소에 저장한다.
- 웹사이트는 Cloudflare Workers(정적 자산 포함)에 배포한다. GitHub와 별도로 CLI(`wrangler
  deploy`)로 직접 배포하며, 코드를 바꾸면 같은 웹 주소에 재배포로 반영한다.
- **동기화(`/api/sync`) 배치 처리**: Worker 요청 하나의 실행 시간 제한 때문에 R2의 전체 노트를
  한 번에 다 처리할 수 없어, 요청당 처리 개수 상한(`limit`, 기본 15)을 두고 여러 번 나눠 호출한다.
  프론트엔드가 `remaining`이 0이 될 때까지 자동으로 반복 호출한다. 이미 분석된 노트는 저장된
  R2 수정 시각과 비교해 건너뛴다(중복 분석 방지).
- **Cron으로 자동 이어하기**: `triggers.crons`(10분마다)로 같은 배치 로직을 주기적으로 실행한다.
  Workers AI 일일 무료 뉴런 할당량을 다 쓴 날에도, 사람이 버튼을 누르지 않아도 할당량이 리셋되는
  순간부터 자동으로 나머지 백필이 이어진다. 처리할 게 없으면 가벼운 조회만 하고 끝나 비용 부담이 적다.
- **태그 목록/검색**: `/api/tags`가 태그별 빈도와 1차 연관 태그(같은 문장에 함께 붙은 태그
  상위 N개)를 SQL로 집계해 반환하고, 검색 자동완성은 이미 받아온 태그 목록을 클라이언트에서
  필터링하는 방식으로 처리한다(포털 검색창처럼 타이핑마다 즉시 추천).
- **토폴로지 시각화**: 별도 백엔드 그래프 DB 없이, 저장된 문장-태그-카테고리 관계를 클라이언트에서
  받아 d3-force로 그린다. 렌더링 전 시뮬레이션을 미리 여러 틱 수렴시킨 뒤 전체 노드의 bounding
  box를 계산해 화면에 자동으로 맞추고(zoom-to-fit), d3-zoom으로 확대/축소·드래그 이동을 지원한다.
  노드 클릭 시 연결된 문장→노트를 역추적해 관련 노트 목록을 보여주고, 노트 선택 시
  `/api/note-content`로 전체 내용(vault 소스는 R2에서, manual 소스는 저장된 원문에서)을 가져와 표시한다.

## 10. 완성 여부를 확인할 기준

- 업로드나 붙여넣기 없이, "동기화된 노트 분석" 실행만으로 R2 `vault/`의 노트 중 아직 분석
  안 된 노트를 찾아 자동으로 단일 사건 문장들로 분리한다 (여러 번 호출해도 전체가 끝까지 처리됨).
- md 파일 업로드나 붙여넣기로도 오류 없이 단일 사건 문장들로 분리된다(보조 경로 확인).
- 분리된 문장마다 태그가 1개 이상, 카테고리가 1개 자동으로 붙는다.
- 이미 분석된 노트는 같은 실행을 다시 돌려도 중복으로 재분석되지 않는다.
- 결과가 Hyperdrive를 통해 Postgres(`chevalut2`)에 저장되어, 새로고침하거나 다시 접속해도 남아 있다.
- 목록 보기에서 태그 빈도 목록과 연관 태그가 보이고, 검색창 타이핑 시 자동완성 추천이 뜨며,
  태그 선택 시 그 태그 중심의 문장 목록과 토폴로지가 함께 갱신된다.
- 토폴로지 보기 진입 시 모든 노드가 한 화면에 자동으로 맞춰 보이고, 확대/축소/드래그 이동이 되며,
  노드 클릭 → 관련 노트 목록 → 노트 선택 → 전체 내용 표시까지 정상 동작한다.
- 배포된 Cloudflare 웹 주소에서 위 과정이 실제로 동작한다.

## 11. 공개 후 확인할 내용

- 실제 본인의 여러 md 노트를 넣었을 때 문장 파싱이 너무 잘게 쪼개지거나 뭉치지 않는지.
- 태그가 노트 내용과 관련 있게 붙는지, 너무 중복되거나 의미 없는 태그가 나오지 않는지.
- 카테고리 분류가 비슷한 내용끼리 일관되게 묶이는지.
- 태그 수가 많아졌을 때(예: 실측 1,200개 이상) 태그 목록/검색 자동완성이 여전히 쓸만한지,
  1차 연관 태그 상위 개수 제한이 적절한지.
- 노트/문장 수가 늘어나도 토폴로지 뷰가 알아보기 힘들 정도로 복잡해지지 않는지.
- 코드 변경 후 다시 배포했을 때, 같은 웹 주소에 정상적으로 반영되는지.
- 데이터가 유실되거나 중복 저장되지 않는지.
- Workers AI 일일 무료 뉴런 할당량으로 전체 볼트(실측 159개 노트) 백필을 끝내기에 충분한지,
  부족하면 유료 플랜 업그레이드가 필요한지.

---

## 참고 — 인프라 식별자

- Cloudflare 계정 ID: `0e0494904ad11d5b5ec5d197a48253ab`
- R2 버킷: `chevault` / 접두사: `vault/` (실측 159개 md 파일, 7개 카테고리 폴더)
- Hyperdrive 구성 이름: `planetscale-chevalut2-main-qacf` / ID: `d57e6622b4d843859f4adccf40a9135d`
- 배포된 Worker 주소: `https://obsidian-summary-agent.cheddochi.workers.dev`
- Workers AI 모델: `@cf/meta/llama-3.1-8b-instruct-fp8`

DB 연결 문자열, Access Key 등 자격 증명은 이 문서에 포함하지 않음 — 필요 시 Cloudflare/PlanetScale
대시보드에서 직접 재확인한다.
