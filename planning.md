# Obsidian Summary Agent planning.md

## 1. 프로젝트 목적

옵시디언에 쌓아온 개인 문서(md 파일)를 자동으로 읽어, 그 안의 내용을 "단일 사건" 단위 문장으로
정리하고 태그와 카테고리를 붙여 저장한다. 목적은 나중에 필요한 정보를 원문 md를 다시 뒤지지 않고
정리된 형태로 빠르게 찾아보는 것이다.

이미 옵시디언 볼트는 별도 파이프라인(chevault-sync)을 통해 Cloudflare Hyperdrive로 연결된
PlanetScale Postgres에 자동으로 동기화되어 쌓이고 있다. 이 프로젝트는 그 동기화된 DB를 기본
데이터 소스로 삼아 분석을 자동 수행하는 것을 우선으로 하고, md 파일 업로드나 클립보드 붙여넣기는
동기화 전이거나 DB에 없는 내용을 즉석에서 분석하고 싶을 때 쓰는 보조 입력 경로로 둔다.

## 2. 주요 사용자

- 나 혼자 사용하는 개인용 도구. 다중 사용자 인증/권한 구분은 필요 없다.

## 3. 해결하려는 문제

- 옵시디언 md 노트는 원문 그대로라 나중에 다시 읽으며 필요한 부분을 찾아야 해서 시간이 든다.
- 하나의 md 파일 안에 여러 사건/내용이 섞여 있어, 검색해도 원하는 부분만 골라보기 어렵다.
- 태그·카테고리를 매번 손으로 붙이기 번거롭다.
- 볼트는 이미 Postgres(Hyperdrive)로 자동 동기화되고 있지만, 그 원문 자체는 여전히 정리되지
  않은 상태로 쌓이기만 할 뿐 분석·요약되지 않는다.

## 4. 꼭 필요한 기능

1. **(기본)** Hyperdrive로 연결된 기존 동기화 Postgres(chevalut2)에서 새로 추가되거나 변경된
   md 노트 내용을 가져와 분석 대상으로 삼는다.
2. **(보조)** md 파일 업로드 또는 클립보드 붙여넣기로 개별 노트 내용을 직접 분석 대상으로 넣는다.
3. md 내용을 "단일 사건" 단위 문장으로 파싱해서 저장
4. 파싱된 문장마다 태그 자동 생성
5. 파싱된 문장마다 카테고리 자동 분류
6. 저장된 문장/태그/카테고리를 목록으로 확인
7. 분석된 문장·태그·카테고리 관계를 토폴로지(네트워크) 형태로 시각화해 한눈에 내용을 식별

## 5. 필요한 화면

- **입력 화면**: (보조 경로) md 파일 업로드 또는 내용 붙여넣기로 개별 노트 분석 실행.
  기본 경로인 DB 자동 분석과는 별개로, 즉석에서 노트 하나를 바로 분석하고 싶을 때 사용.
- **결과 확인 화면**: 파싱된 문장 목록 + 각 문장의 태그, 카테고리 표시.
  DB에서 자동 수집·분석된 노트와 업로드/붙여넣기로 분석한 노트를 구분 없이 함께 보여준다
  (각 문장이 어느 원본 노트에서 나왔는지는 표시).
  - 목록 보기 / 토폴로지(관계도) 보기 두 가지 방식을 한 화면에서 전환 가능하게 구성
  - 토폴로지 보기: 문장·태그·카테고리를 노드로, 문장-태그·문장-카테고리 관계를 연결선으로 표시해
    어떤 문장이 어떤 태그/카테고리에 속하는지 시각적으로 바로 식별할 수 있게 한다

### 원하는 화면 분위기

첨부된 `design.md`(오브시디언 공식 사이트 톤 기반 디자인 가이드)의 방향을 반영한다.
목록 보기는 색/타이포/톤 위주로, 토폴로지 보기는 design.md의 그래프 뷰(4-2) 스타일을 그대로 적용한다.

- **톤 앤 매너**: 화려한 마케팅 톤이 아니라 조용하고 차분한 도구 느낌. 미니멀, 여백 많음, 불필요한 장식 없음.
- **베이스 컬러(다크 모드 기본)**: 배경은 순검정이 아닌 짙은 차콜(`#1e1e1e`~`#202020`), 카드/패널은
  한 톤 밝은 `#262626`~`#2a2a2a`, 기본 텍스트는 순백 대신 톤 낮춘 회백색(`#dcddde`), 보조 텍스트는
  muted gray(`#9a9a9a`), 테두리는 거의 안 보이는 `#3a3a3a`.
- **포인트 컬러**: 퍼플(`#7c3aed` 베이스, 버튼/헤드라인용 그라디언트 `#8a4fff → #b76bff`)을
  강조·CTA·태그 하이라이트에만 아껴서 사용하고, 나머지 UI는 그레이스케일로 절제.
- **타이포그래피**: 본문/UI는 시스템 산세리프(`Inter` 등), 헤딩은 굵게 웨이트 대비로 임팩트.
  태그·원문 경로 같은 기술적 요소는 모노스페이스(`JetBrains Mono` 등)로 포인트.
- **결과 확인 화면 스타일**: 문장 하나하나를 카드/리스트 아이템으로, radius `12px` 안팎에
  옅은 테두리(`1px solid #3a3a3a`)로 구분. 태그는 알약(pill) 형태(배경 `#333`, 텍스트 `#a48fff`).
  카테고리는 채도 낮춘 보조색으로 배지 표시.
- **버튼**: Primary(파싱 실행 등)는 퍼플 그라디언트 배경, Secondary는 배경 없이 테두리만.
  담백한 카피 사용("파싱하기", "결과 보기" 등), 과장된 마케팅 문구 지양.
- **토폴로지 보기 스타일**: 배경은 순수 다크(`#161616`), 노드(문장/태그/카테고리)는 은은하게
  빛나는 점으로, 연결선은 얇고 반투명(`stroke-opacity: 0.15~0.3`)하게 표시. 기본 노드는
  muted gray(`#666`), 선택되거나 강조할 노드는 퍼플 글로우(`#a48fff`)로 구분. 카테고리별로
  노드가 자연스럽게 뭉치도록 force-directed 레이아웃을 사용하고, 노드 클릭 시 해당 문장 상세를
  옆 패널에 표시. 태그/카테고리 노드는 문장 노드와 형태(색·크기)를 다르게 해 한눈에 구분되게 한다.
- **모드**: 다크 모드를 기본값으로 하고 시작한다(라이트 모드 토글이나 테마 커스터마이징 기능 자체는
  8번에 따라 이번에는 만들지 않음 — 다크 테마 하나만 고정으로 적용).

## 6. 저장할 정보

- 단일 사건 문장 (원문에서 파싱된 문장 텍스트)
- 태그 (문장별 자동 생성된 태그, 다대다 관계)
- 카테고리 (문장별 자동 분류된 카테고리 1개)
- 원본 노트 참조 정보 (DB에서 가져온 노트는 기존 `notes` 테이블의 경로/식별자, 업로드·붙여넣기는
  파일명 또는 "붙여넣기 + 분석 시각"으로 구분해 저장 — 문장이 어디서 나왔는지 추적하기 위함)
- 마지막으로 분석을 완료한 노트의 동기화 시각(기존 `notes.r2_modified_at` 또는 `synced_at` 기준)
  — 이미 분석한 노트를 다음 자동 실행에서 중복 분석하지 않기 위한 최소한의 기준값

원본 md 내용 자체는 이미 기존 Postgres(`chevalut2`)의 `notes` 테이블에 있으므로 이 프로젝트가
다시 저장하지 않는다. 이 프로젝트의 Cloudflare D1에는 분석 결과(문장/태그/카테고리와 원본 참조)만
저장한다. 단, 업로드·붙여넣기로 들어온 내용은 기존 DB에 없으므로 원문도 함께 D1에 보관한다.

## 7. 사용자가 서비스를 이용하는 순서

1. **(기본)** 별도 조작 없이도, Hyperdrive로 연결된 기존 동기화 Postgres에 새로 들어오거나
   변경된 노트가 있으면 서비스가 이를 가져와 자동으로 분석한다(수동 실행 버튼 또는 주기 실행).
2. **(보조)** 특정 노트를 바로 분석하고 싶으면 md 파일을 업로드하거나 내용을 붙여넣어
   수동으로 분석을 실행할 수 있다.
3. 분석이 실행되면 서비스가 내용을 단일 사건 단위 문장으로 분리한다.
4. 분리된 문장마다 태그와 카테고리가 자동으로 붙어 저장된다.
5. 결과 확인 화면에서 저장된 문장/태그/카테고리 목록을 확인한다.
6. 필요하면 토폴로지 보기로 전환해 문장-태그-카테고리 관계를 시각적으로 탐색한다.

## 8. 이번에 만들지 않을 기능

- 여러 사용자를 위한 로그인/권한 관리
- 옵시디언 볼트 → R2 → Postgres 동기화 파이프라인 자체를 새로 만들거나 수정하는 것
  (chevault-sync가 이미 이 역할을 하고 있음 — 이 프로젝트는 그 결과물인 Postgres를
  Hyperdrive로 읽기 전용 연결만 한다)
- R2 변경 즉시 반응하는 실시간 스트리밍 분석 (이번에는 수동 실행 버튼 또는 주기적 폴링으로 충분)
- 옵시디언 위키링크 기반 노트 간 관계 그래프 (이 프로젝트는 위키링크를 다루지 않음 —
  문장·태그·카테고리 관계를 보여주는 토폴로지 시각화는 4번 기능으로 포함됨)
- 태그·카테고리를 손으로 고치는 편집 UI (자동 생성 결과만 저장)
- 전문 검색, 추천 검색 등 고급 검색 기능
- 라이트 모드 토글 등 테마 커스터마이징 기능 (다크 테마 하나만 고정 적용)

## 9. 기술 및 배포 방향

- 저장할 데이터가 있으므로 Cloudflare D1에 저장한다 (여러 사람이 같은 데이터를 쓸 수 있는 구조로 준비하되, 현재는 1인 사용).
- 기존에 구축된 Cloudflare Hyperdrive(`planetscale-chevalut2-main-qacf`)를 통해 PlanetScale
  Postgres(`chevalut2`)의 `notes` 테이블을 **읽기 전용**으로 조회해, 자동 동기화된 md 원문을
  분석 대상으로 가져온다. 이 프로젝트는 해당 DB에 쓰기 작업을 하지 않는다.
- 프로젝트 파일과 변경 내용은 GitHub 저장소에 저장한다.
- 웹사이트는 Cloudflare(Workers/Pages)에 배포한다.
- GitHub 저장소와 Cloudflare 배포를 연동해, 이후 코드를 변경해도 같은 웹 주소에 반영되도록 구성한다.
- Worker 하나에 Hyperdrive 바인딩(읽기: 기존 노트 조회)과 D1 바인딩(쓰기: 분석 결과 저장)을
  함께 구성해, md 조회 → 문장 분리 → 태그/카테고리 생성까지 처리하는 최소 구조로 시작한다
  (오늘 안에 완성 가능한 범위 유지).
- 자동 분석 실행은 "결과 확인 화면의 분석 실행 버튼" 또는 간단한 cron 중 하나로 최소 구현하고,
  이미 분석된 노트는 저장된 동기화 시각과 비교해 건너뛴다(중복 분석 방지).
- 토폴로지 시각화는 별도 백엔드 그래프 DB 없이, 저장된 문장-태그-카테고리 관계를 클라이언트에서
  받아 d3-force 등 가벼운 force-directed 레이아웃 라이브러리로 그리는 방식으로 최소 구현한다.

## 10. 완성 여부를 확인할 기준

- 업로드나 붙여넣기 없이, 분석 실행만으로 Hyperdrive에 연결된 기존 Postgres의 노트 중
  아직 분석 안 된 노트를 찾아 자동으로 단일 사건 문장들로 분리한다.
- md 파일 업로드나 붙여넣기로도 오류 없이 단일 사건 문장들로 분리된다(보조 경로 확인).
- 분리된 문장마다 태그가 1개 이상 자동으로 붙는다.
- 분리된 문장마다 카테고리가 1개 자동으로 분류되어 표시된다.
- 이미 분석된 노트는 같은 실행을 다시 돌려도 중복으로 재분석되지 않는다.
- 결과가 Cloudflare D1에 저장되어, 새로고침하거나 다시 접속해도 남아 있다.
- 결과 확인 화면에서 토폴로지 보기로 전환하면 문장·태그·카테고리가 노드/연결선으로 표시되고,
  어떤 문장이 어떤 태그·카테고리에 속하는지 클릭 없이도 시각적으로 구분된다.
- 배포된 Cloudflare 웹 주소에서 위 과정이 실제로 동작한다.

## 11. 공개 후 확인할 내용

- 실제 본인의 여러 md 노트를 넣었을 때 문장 파싱이 너무 잘게 쪼개지거나 뭉치지 않는지.
- 태그가 노트 내용과 관련 있게 붙는지, 너무 중복되거나 의미 없는 태그가 나오지 않는지.
- 카테고리 분류가 비슷한 내용끼리 일관되게 묶이는지.
- 노트/문장 수가 늘어나도 토폴로지 뷰가 알아보기 힘들 정도로 복잡해지지 않는지, 노드가 너무 많으면
  어떻게 정리해서 보여줄지 추가 검토가 필요한지.
- 코드 변경 후 다시 배포했을 때, 같은 웹 주소에 정상적으로 반영되는지.
- 데이터가 유실되거나 중복 저장되지 않는지.
- 실제 동기화된 볼트 전체 노트 수 기준으로 자동 분석이 합리적인 시간 안에 끝나는지,
  Hyperdrive 조회량이 많아졌을 때도 안정적으로 동작하는지.

---

## 참고 — 인프라 식별자

9번에서 언급한 Hyperdrive 연결에 실제로 사용할 값. Cloudflare 대시보드
(Storage & Databases → Postgres 및 MySQL(Hyperdrive))에서 확인함.

- Hyperdrive 구성 이름: `planetscale-chevalut2-main-qacf`
- Hyperdrive 구성 ID: `d57e6622b4d843859f4adccf40a9135d`
- Worker 코드 작성 시 `wrangler.jsonc`의 `hyperdrive[0].id`에 위 구성 ID를 넣어 바인딩한다.

DB 연결 문자열, Access Key 등 자격 증명은 이 문서에 포함하지 않음 — 필요 시 Cloudflare/PlanetScale
대시보드에서 직접 재확인한다.
